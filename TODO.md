# TODOリスト: MATLAB風C++ライブラリ (Eigen Wrapper) の実装計画

## 要求仕様.mdに基づく未実装機能

### 1. コロン演算子の実装
- [x] `Mat::linspace(start, end, n)` の実装
  - 線形スペースを生成する静的メソッド
  - MATLABの `linspace(start, end, n)` に相当
- [x] `Mat::range(start, step, end)` の実装
  - ステップサイズを指定して範囲を生成する静的メソッド
  - MATLABの `start:step:end` に相当

### 2. 統計関数の実装（次元指定対応）
- [x] `mean()` メソッドの実装
  - 次元を指定できる平均値計算（MATLABの `mean(A, dim)`）
  - 次元指定がない場合は全要素の平均
- [x] `std()` メソッドの実装
  - 次元を指定できる標準偏差計算（MATLABの `std(A, dim)`）
  - 次元指定がない場合は全要素の標準偏差
- [x] `sum()` メソッドの実装
  - 次元を指定できる合計計算（MATLABの `sum(A, dim)`）
 - [x] `max()`、`min()` メソッドの実装
  - 次元を指定できる最大値・最小値計算（MATLABの `max(A, dim)`、`min(A, dim)`）
  - 次元指定がない場合は全要素の最大値・最小値
- [ ] `median()` メソッドの実装
  - 次元を指定できる中央値計算（MATLABの `median(A, dim)`）
  - 次元指定がない場合は全要素の中央値
- [ ] `prod()` メソッドの実装
  - 次元を指定できる積計算（MATLABの `prod(A, dim)`）
  - 次元指定がない場合は全要素の積
- [ ] `cumsum()`、`cumprod()` メソッドの実装
  - 次元を指定できる累積和・累積積計算（MATLABの `cumsum(A, dim)`、`cumprod(A, dim)`）

### 3. 論理インデックスの実装
- [ ] `find()` 関数の実装
  - 非ゼロ要素の線形インデックスを返す（MATLABの `find(A)`）
  - 条件式に対応したバージョンも考慮（例: `find(A > 0)`）
- [ ] `find()` 関数のオーバーロード
  - 行列インデックスを返すバージョンも実装（MATLABの `[i,j] = find(A)`）
  - 条件式に対応したバージョン（例: `find(A > 0, 'lin')`）
- [ ] 論理インデックス演算子のサポート
  - `A(A > 0)` のような式をサポートするための仕組み
  - マスク行列を使用した要素抽出機能
  - 論理演算結果をマスクとして使用する仕組み（`A & B`, `A | B` など）

### 4. 基本ベクトル・行列関数の実装
- [ ] `trace()` メソッドの実装
  - 正方行列の対角要素の和を計算（MATLABの `trace(A)`）
- [ ] `norm()` メソッドの実装
  - 行列またはベクトルのノルムを計算（MATLABの `norm(A)`）
  - フロベニウスノルム、L2ノルムなどをサポート
- [ ] `reshape()` メソッドの実装
  - 行列の形状を変更しながらデータを保持（MATLABの `reshape(A, m, n)`）
  - 要素数が一致している必要がある
- [ ] `flip()` メソッドの実装
  - 次元を指定して要素を反転（MATLABの `flip(A, dim)`）
  - 上下左右反転をサポート
- [ ] `rot90()` メソッドの実装
  - 90度単位で行列を回転（MATLABの `rot90(A, k)`）
  - 反時計回りに k*90 度回転
- [ ] `repmat()` メソッドの実装
  - 行列をブロック状に複製（MATLABの `repmat(A, m, n)`）
  - 大きなブロック行列を効率的に作成
- [ ] `blkdiag()` メソッドの実装
  - 入力行列を対角ブロックとするブロック対角行列を作成（MATLABの `blkdiag(A,B,C,...)`）
- [ ] `triu()`、`tril()` メソッドの実装
  - 上三角・下三角部分を抽出（MATLABの `triu(A)`、`tril(A)`）
  - 対角線を含むかどうかをオプションで指定可能
- [ ] `diag()` メソッドの実装
  - ベクトルから対角行列を作成、または行列から対角成分を抽出（MATLABの `diag(V)`、`diag(A)`）
  - 対角線のオフセットを指定可能
- [ ] `fliplr()`、`flipud()` メソッドの実装
  - 左右反転・上下反転（MATLABの `fliplr(A)`、`flipud(A)`）
  - 特殊ケースのflip関数として実装
- [x] `dot()` メソッドの実装
  - 2つのベクトルの内積（ドット積）を計算（MATLABの `dot(A, B)`）
  - 次元が一致している必要がある
- [x] `cross()` メソッドの実装
  - 2つの3次元ベクトルの外積（クロス積）を計算（MATLABの `cross(A, B)`）
  - 両方とも3次元ベクトルである必要がある

### 5. テストの作成
- [ ] `tests/` ディレクトリの作成
- [ ] 各機能に対応するユニットテストの作成
  - 構築・生成関数のテスト
  - 基本演算のテスト
  - 数値解析関数のテスト
  - 拡張機能（統計関数、論理インデックス、ベクトル/行列関数）のテスト